#include "parts.h"

// float SnakePart::computeDistance(const SnakePart other_part) {
// 	sf::Vector2f diff = getCoordinates() - other_part.getCoordinates();
// 	float dist = sqrt(diff.x*diff.x+diff.y*diff.y);
// }

